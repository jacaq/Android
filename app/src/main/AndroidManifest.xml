<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crosemont.guichetautomatiqueatm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuichetAutomatiqueATM">
        <activity android:name=".Principal_creation_client"></activity>
        <activity android:name=".Principal_creation_epargne"></activity>
        <activity android:name=".Principal_creation_cheque"></activity>
        <activity android:name=".Administrateur"></activity>
        <activity android:name=".Bienvenue_Transactions" />
        <activity android:name=".MainActivity_Pass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>